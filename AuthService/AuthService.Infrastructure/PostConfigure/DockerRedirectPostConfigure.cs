// This file previously provided optional host.docker.internal redirect URI overrides.
// The project currently uses the configured RedirectUri (localhost) for local development
// and therefore these post-configure helpers are no longer necessary and are intentionally left blank.
