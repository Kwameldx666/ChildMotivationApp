# Motivation Up App — Frontend

## Документация
- `docs/README.md`

## Быстрый старт
1. Заполните `NEXT_PUBLIC_API_URL` в `.env.local` (пример — `.env.example`).
2. Установите зависимости и запустите:
   - `npm i`
   - `npm run dev`

